-------------------------------------------------------------------------------------------------------------------------------------------
---------------------------------------------------- Assignment No 3 ----------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------

Create a file named "data.txt" and write the following lines in it(write as it is).
Linux is open source.
In Linux everything is file
files have permissions.
files have inode no.
files have size.
there are several types of files

compose the commands fulfilled following requirement

i.count the no. of words ,characters and lines from above 2 file.(man wc)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ wc data.txt 

->  6  25 144 data.txt

-------------------------------------------------------------------------------------------------------------------------------------------
ii.list the lines having word "files" (man grep)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ egrep "files" data.txt 
files have permissions.
files have inode no.
files have size
there are several types of files

-------------------------------------------------------------------------------------------------------------------------------------------
iii.list the lines having word "file" (man grep)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ egrep "^file" data.txt 
files have permissions.
files have inode no.
files have size

-------------------------------------------------------------------------------------------------------------------------------------------
iv.list the lines which don't have word "files" (man grep)

->

-------------------------------------------------------------------------------------------------------------------------------------------
v.list the lines having word "have" . (man grep)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ egrep "have" data.txt files have permissions.
files have inode no.
files have size

-------------------------------------------------------------------------------------------------------------------------------------------
vi.list the lines starts with letter "f" (man grep)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ egrep "^f" data.txt 
files have permissions.
files have inode no.
files have size

-------------------------------------------------------------------------------------------------------------------------------------------
vii.list the lines ends with "." (man grep)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ egrep "\.$" data.txt 
Linux is open source.
files have permissions.
files have inode no.

-------------------------------------------------------------------------------------------------------------------------------------------
viii.list only first two lines.(man head)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ head -2 data.txt 
Linux is open source.
In Linux everything is file

-------------------------------------------------------------------------------------------------------------------------------------------
ix.list only last three lines.(man tail)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ tail -3 data.txt 
files have inode no.
files have size
there are several types of files

-------------------------------------------------------------------------------------------------------------------------------------------
x.list line no.3,4 and 5 . (man head and tail)

->sunbeam@sunbeam-HP-15-TS-Notebook-PC:~/Desktop$ tail -5 data.txt | tail -3
files have inode no.
files have size
there are several types of files

-------------------------------------------------------------------------------------------------------------------------------------------










